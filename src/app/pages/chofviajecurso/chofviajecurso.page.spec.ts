import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ChofviajecursoPage } from './chofviajecurso.page';

describe('ChofviajecursoPage', () => {
  let component: ChofviajecursoPage;
  let fixture: ComponentFixture<ChofviajecursoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ChofviajecursoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
