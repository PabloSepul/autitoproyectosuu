import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ChoferprogconfirmarPage } from './choferprogconfirmar.page';

describe('ChoferprogconfirmarPage', () => {
  let component: ChoferprogconfirmarPage;
  let fixture: ComponentFixture<ChoferprogconfirmarPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ChoferprogconfirmarPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
