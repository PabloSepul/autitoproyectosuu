import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ChoferprognotiPage } from './choferprognoti.page';

describe('ChoferprognotiPage', () => {
  let component: ChoferprognotiPage;
  let fixture: ComponentFixture<ChoferprognotiPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ChoferprognotiPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
